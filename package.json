{
  "name": "avocado-extra-docs",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check:links": "node scripts/check-links.mjs",
    "check:schema": "node scripts/check-schema.mjs",
    "check": "npm run check:schema && npm run build && npm run check:links"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.42.4",
    "astro": "^7.2.10",
    "sharp": "^0.35.3",
    "starlight-llms-txt": "^0.12.0"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "yaml": "^2.9.1"
  },
  "description": "Unofficial, source-verified docs for the avocado CLI and Avocado OS",
  "private": true,
  "engines": {
    "node": ">=22.12"
  }
}
